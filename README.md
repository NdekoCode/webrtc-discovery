# The *WebRTC*

 The **WebRTC** is a framework that allows the implementation of an instant communication system directly in the browser. It brings different components within the browser that allows everyone to implement their own RTC application
